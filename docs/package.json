{
  "name": "spore-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/sync-changelog.mjs && vitepress dev --port 5163",
    "build": "node scripts/sync-changelog.mjs && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
